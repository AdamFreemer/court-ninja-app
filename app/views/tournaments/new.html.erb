<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New <%= @type == 'adhoc' ? 'Adhoc ' : '' %>Tournament</h1>
  <% if @type == "adhoc" %>
    <%= render "form_adhoc", tournament: @tournament %>
  <% else %>
    <%= render "form", tournament: @tournament %>
  <% end %>
</div>