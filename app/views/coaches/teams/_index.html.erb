<div class="flex-1">
  <div class="flex justify-between items-center border-b-2 pb-2">
    <h2 class="font-semibold text-3xl">My Teams</h2>
    <%= link_to 'Add Team', new_team_path, class: "inline-flex items-center px-2.5 py-1.5 border border-transparent text-xs font-medium rounded text-indigo-700 bg-indigo-100 hover:bg-indigo-200 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" if current_user.teams_coached.count < 1 %>
  </div>
  <% if current_user.teams_coached.count == 0 %>
    <div class="shadow overflow-hidden bg-gray-50 py-10 text-center rounded-sm">
      You have no teams. Click "Create Team" above to add your team and invite your players.<br>
      <i>Note: You must have a team with players to create a non-adhoc tournament.</i>
    </div>
  <% else %>
    <%= render "coaches/teams/table" %>
  <% end %>
</div>